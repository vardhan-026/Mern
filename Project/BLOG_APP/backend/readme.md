1. create git repo
    git init

2. add .gitignore

3. create .env for environment variables and read .env with 'dotenv' module
    npm install dotenv

4. generate package.json
    npm init -y

5. create express application
    npm install express

6. connection to db
    npm install mongoose

7. add middlewares[body-parser,error handing middleware]

8. design schema and create modules

9. design api's for all resources

Registrtion and Login

10.Registration and Login in common